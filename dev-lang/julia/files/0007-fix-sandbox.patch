--- a/Makefile
+++ b/Makefile
@@ -334,7 +333,7 @@ endif
 	done
 	for suffix in $(JL_PRIVATE_LIBS-1) ; do \
 		lib=$(build_private_libdir)/$${suffix}.$(SHLIB_EXT); \
-		$(INSTALL_M) $$lib $(DESTDIR)$(private_libdir) ; \
+		cp -a $$lib $(DESTDIR)$(private_libdir) ; \
 	done
 endif
 
