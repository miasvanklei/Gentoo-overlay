--- a/shared/nm-glib-aux/nm-errno.c	2019-11-30 00:12:48.463895049 +0100
+++ b/shared/nm-glib-aux/nm-errno.c	2019-11-30 00:14:06.855720102 +0100
@@ -121,21 +121,13 @@
 	nm_assert (buf);
 	nm_assert (buf_size > 0);
 
-#if (_POSIX_C_SOURCE >= 200112L) && !  _GNU_SOURCE
-	/* XSI-compliant */
-	{
-		int errno_saved = errno;
+	int errno_saved = errno;
 
-		if (strerror_r (errsv, buf, buf_size) != 0) {
-			g_snprintf (buf, buf_size, "Unspecified errno %d", errsv);
-			errno = errno_saved;
-		}
-		buf2 = buf;
+	if (strerror_r (errsv, buf, buf_size) != 0) {
+		g_snprintf (buf, buf_size, "Unspecified errno %d", errsv);
+		errno = errno_saved;
 	}
-#else
-	/* GNU-specific */
-	buf2 = strerror_r (errsv, buf, buf_size);
-#endif
+	buf2 = buf;
 
 	/* like g_strerror(), ensure that the error message is UTF-8. */
 	if (   !g_get_charset (NULL)
