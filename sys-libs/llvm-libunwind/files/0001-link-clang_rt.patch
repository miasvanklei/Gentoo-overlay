--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -213,6 +213,7 @@
 
 if (LIBUNWIND_USE_COMPILER_RT)
   list(APPEND LIBUNWIND_LINK_FLAGS "-rtlib=compiler-rt")
+  list(APPEND LIBUNWIND_LINK_FLAGS "${LIBUNWIND_BUILTINS_LIBRARY}")
 endif()
 
 #===============================================================================
