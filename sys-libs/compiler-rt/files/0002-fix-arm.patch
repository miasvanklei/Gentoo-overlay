--- lib/builtins/clear_cache.c.bak	1497819191.361252131
+++ lib/builtins/clear_cache.c	1497818066.451251594
@@ -116,9 +116,6 @@
                           : "=r"(start_reg)
                           : "r"(syscall_nr), "r"(start_reg), "r"(end_reg),
                             "r"(flags));
-         if (start_reg != 0) {
-             compilerrt_abort();
-         }
     #elif defined(_WIN32)
         FlushInstructionCache(GetCurrentProcess(), start, end - start);
     #else
