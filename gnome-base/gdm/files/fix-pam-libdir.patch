--- a/pam_gdm/Makefile.am	2016-09-07 20:34:38.000000000 +0200
+++ b/pam_gdm/Makefile.am.	2016-10-05 11:53:04.801866866 +0200
@@ -36,4 +36,4 @@
 	pam_gdm.la \
 	$(END_OF_LIST)
 
-pam_gdmdir = $(libdir)/security
+pam_gdmdir = $(SECUREDIR)
--- a/configure.ac	2016-10-05 12:07:11.606847251 +0200
+++ b/configure.ac	2016-10-05 12:17:45.786832562 +0200
@@ -178,6 +178,12 @@
 dnl ---------------------------------------------------------------------------
 dnl - Configure arguments
 dnl ---------------------------------------------------------------------------
+AC_ARG_ENABLE(securedir,
+              AS_HELP_STRING([--enable-securedir=DIR],
+                             [Path to location of PAMs @<:@default=$libdir/security@:>@]),
+              SECUREDIR=$enableval, SECUREDIR=$libdir/security)
+AC_SUBST(SECUREDIR)
+
 AC_ARG_ENABLE(gdm-xsession,
               AS_HELP_STRING([--enable-gdm-xsession],
                              [Enable installing the gdm Xsession file @<:@default=no@:>@]),,
@@ -1561,6 +1562,7 @@
 	prefix:                   ${prefix}
 	exec_prefix:              ${exec_prefix}
         libdir:                   ${libdir}
+        pamlibdir:                ${SECUREDIR}
         bindir:                   ${bindir}
         sbindir:                  ${sbindir}
         sysconfdir:               ${sysconfdir}
