--- a/make/autoconf/flags-cflags.m4	2021-04-05 11:11:41.066125897 +0200
+++ b/make/autoconf/flags-cflags.m4	2021-04-05 11:17:16.968196019 +0200
@@ -726,6 +726,9 @@
         $1_CFLAGS_CPU_JDK="${$1_CFLAGS_CPU_JDK} -fno-omit-frame-pointer"
       fi
     fi
+    # Clang > 12, applies 'nonnull' and 'dereferencable(N)' to 'this' pointer arguments
+    # causing openjdk to segfault disable these optimizations here
+    $1_TOOLCHAIN_CFLAGS="-fno-delete-null-pointer-checks"
 
   elif test "x$TOOLCHAIN_TYPE" = xxlc; then
     if test "x$FLAGS_CPU" = xppc64; then
