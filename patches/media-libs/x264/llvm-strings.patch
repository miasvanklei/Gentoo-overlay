--- a/configure	2017-11-09 12:01:12.938944674 +0000
+++ b/configure	2017-11-09 12:01:32.939883111 +0000
@@ -963,10 +963,10 @@
 if [ $compiler = GNU ]; then
     echo "int i[2] = {0x42494745,0}; double f[2] = {0x1.0656e6469616ep+102,0};" > conftest.c
     $CC $CFLAGS conftest.c -c -o conftest.o 2>/dev/null || die "endian test failed"
-    if (${cross_prefix}strings -a conftest.o | grep -q BIGE) && (${cross_prefix}strings -a conftest.o | grep -q FPendian) ; then
+    if (${cross_prefix}strings conftest.o | grep -q BIGE) && (${cross_prefix}strings conftest.o | grep -q FPendian) ; then
         define WORDS_BIGENDIAN
         CPU_ENDIAN="big-endian"
-    elif !(${cross_prefix}strings -a conftest.o | grep -q EGIB && ${cross_prefix}strings -a conftest.o | grep -q naidnePF) ; then
+    elif !(${cross_prefix}strings conftest.o | grep -q EGIB && ${cross_prefix}strings conftest.o | grep -q naidnePF) ; then
         die "endian test failed"
     fi
 fi
