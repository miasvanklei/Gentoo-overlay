--- a/GNUmakefile	2017-10-13 20:56:25.974950849 +0200
+++ b/GNUmakefile	2017-10-13 20:58:29.618818479 +0200
@@ -12,7 +12,6 @@
 CHMOD ?= chmod
 MKDIR ?= mkdir
 LN ?= ln -sf
-LDCONF ?= /sbin/ldconfig -n
 UNAME := $(shell uname)
 
 IS_X86 := $(shell uname -m | $(EGREP) -v "x86_64" | $(EGREP) -i -c "i.86|x86|i86")
@@ -580,8 +579,8 @@
 	$(CP) libcryptopp.so$(SOLIB_VERSION_SUFFIX) $(DESTDIR)$(LIBDIR)
 	-$(CHMOD) 0755 $(DESTDIR)$(LIBDIR)/libcryptopp.so$(SOLIB_VERSION_SUFFIX)
 ifeq ($(HAS_SOLIB_VERSION),1)
-	-$(LN) -sf libcryptopp.so$(SOLIB_VERSION_SUFFIX) $(DESTDIR)$(LIBDIR)/libcryptopp.so
-	$(LDCONF) $(DESTDIR)$(LIBDIR)
+	-$(LN) -sf libcryptopp.so$(SOLIB_VERSION_SUFFIX) $(DESTDIR)$(LIBDIR)/libcryptopp.so${SOLIB_COMPAT_SUFFIX}
+	-$(LN) -sf libcryptopp.so$(SOLIB_COMPAT_SUFFIX) $(DESTDIR)$(LIBDIR)/libcryptopp.so
 endif
 endif
 
@@ -598,7 +597,6 @@
 ifeq ($(HAS_SOLIB_VERSION),1)
 	-$(RM) $(DESTDIR)$(LIBDIR)/libcryptopp.so$(SOLIB_COMPAT_SUFFIX)
 	-$(RM) $(DESTDIR)$(LIBDIR)/libcryptopp.so
-	$(LDCONF) $(DESTDIR)$(LIBDIR)
 endif
 endif
 
