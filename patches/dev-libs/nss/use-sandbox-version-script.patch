--- a/coreconf/Linux.mk	2016-10-16 16:19:05.888878597 +0200
+++ b/coreconf/Linux.mk	2016-10-16 16:19:25.188877009 +0200
@@ -110,7 +110,7 @@
 	OS_REL_CFLAGS	+= -DLINUX2_0
 	MKSHLIB		= $(CC) -shared -Wl,-soname -Wl,$(@:$(OBJDIR)/%.so=%.so) $(RPATH)
 	ifdef MAPFILE
-		MKSHLIB += -Wl,--version-script,$(MAPFILE)
+		MKSHLIB += -Wl,--sandbox-version-script,$(MAPFILE)
 	endif
 	PROCESS_MAP_FILE = grep -v ';-' $< | \
          sed -e 's,;+,,' -e 's; DATA ;;' -e 's,;;,,' -e 's,;.*,;,' > $@
@@ -206,7 +206,7 @@
 MKSHLIB         = $(CC) $(DSO_LDOPTS) -Wl,-soname -Wl,$(@:$(OBJDIR)/%.so=%.so) $(RPATH)
 
 ifdef MAPFILE
-	MKSHLIB += -Wl,--version-script,$(MAPFILE)
+	MKSHLIB += -Wl,--sandbox-version-script,$(MAPFILE)
 endif
 PROCESS_MAP_FILE = grep -v ';-' $< | \
         sed -e 's,;+,,' -e 's; DATA ;;' -e 's,;;,,' -e 's,;.*,;,' > $@
