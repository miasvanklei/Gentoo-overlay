--- include/uapi/linux/if_ether.h	2015-06-23 15:04:38.000000000 +0000
+++ include/uapi/linux/if_ether.h	2015-08-20 11:28:37.875454420 +0000
@@ -134,11 +134,14 @@
  *	This is an Ethernet frame header.
  */
 
+#if !defined(HAVE_IF_ETHER)
+#define HAVE_IF_ETHER
 struct ethhdr {
 	unsigned char	h_dest[ETH_ALEN];	/* destination eth addr	*/
 	unsigned char	h_source[ETH_ALEN];	/* source ether addr	*/
 	__be16		h_proto;		/* packet type ID field	*/
 } __attribute__((packed));
+#endif
 

 #endif /* _UAPI_LINUX_IF_ETHER_H */
--- include/uapi/linux/if_arp.h	2015-06-23 15:04:38.000000000 +0000
+++ include/uapi/linux/if_arp.h	2015-08-20 11:37:56.962823172 +0000
@@ -110,6 +110,8 @@
 
 
 /* ARP ioctl request. */
+#if !defined(HAVE_ARP_H)
+#define HAVE_ARP_H
 struct arpreq {
   struct sockaddr	arp_pa;		/* protocol address		*/
   struct sockaddr	arp_ha;		/* hardware address		*/
@@ -154,8 +156,9 @@
 	unsigned char		ar_tha[ETH_ALEN];	/* target hardware address	*/
 	unsigned char		ar_tip[4];		/* target IP address		*/
 #endif
+#endif
 
 };
 
 
 #endif /* _UAPI_LINUX_IF_ARP_H */
--- include/uapi/linux/in6.h.bak	2015-09-05 14:40:48.524818480 +0200
+++ include/uapi/linux/in6.h	2015-09-05 14:44:26.121812417 +0200
@@ -28,6 +28,8 @@
  *	IPv6 address structure
  */
 
+#if !defined(HAVE_IN6_H)
+#define HAVE_IN6_H
 #if __UAPI_DEF_IN6_ADDR
 struct in6_addr {
 	union {
@@ -64,6 +66,7 @@
 	int		ipv6mr_ifindex;
 };
 #endif /* __UAPI_DEF_IVP6_MREQ */
+#endif
 
 #define ipv6mr_acaddr	ipv6mr_multiaddr
 
