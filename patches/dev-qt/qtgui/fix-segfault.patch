--- a/src/gui/text/qfont.cpp	2015-10-16 23:29:40.578350561 +0200
+++ b/src/gui/text/qfont.cpp	2015-10-16 23:30:00.401284804 +0200
@@ -137,8 +137,8 @@
     if (!qt_is_gui_used)
         return 75;
 
-    if (const QScreen *screen = QGuiApplication::primaryScreen())
-        return qRound(screen->logicalDotsPerInchX());
+//    if (const QScreen *screen = QGuiApplication::primaryScreen())
+//        return qRound(screen->logicalDotsPerInchX());
 
     //PI has not been initialised, or it is being initialised. Give a default dpi
     return 100;
@@ -152,8 +152,8 @@
     if (!qt_is_gui_used)
         return 75;
 
-    if (const QScreen *screen = QGuiApplication::primaryScreen())
-        return qRound(screen->logicalDotsPerInchY());
+ //   if (const QScreen *screen = QGuiApplication::primaryScreen())
+ //       return qRound(screen->logicalDotsPerInchY());
 
     //PI has not been initialised, or it is being initialised. Give a default dpi
     return 100;
